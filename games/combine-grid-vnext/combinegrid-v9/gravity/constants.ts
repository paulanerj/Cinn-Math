
export const GRAVITY_PHYSICS = {
  GRAVITY: 1.8,
  FRICTION: 0.90,
  BOUNCE: 0.20,
  SNAP: 0.8,
  SPAWN_OFFSET: 350,
  /** Spawns start descending after this delay (ms) to allow moves to lead. */
  PHASE_SPAWN_DELAY_MS: 120,
};
