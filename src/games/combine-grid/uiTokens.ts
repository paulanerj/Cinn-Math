/** CombineGrid locked layout tokens — DO NOT edit without updating COMBINE_GRID_UI_CONTRACT.md */
export const SAFE_MARGIN = 5;   // px — outer safe area margin
export const BORDER_WIDTH = 5;  // px — tile border
export const GAP = 2;           // px — grid gap
export const PAD = 6;           // px — tile inner padding
export const BASE_RADIUS_PX = 16; // px — tile border radius
export const GRID_SCALE = 1.03; // subtle scale-up on the grid container

/** HUD heights (must match HUDShell) */
export const HUD_TOP_H = 54;
export const HUD_BOT_H = 52;
