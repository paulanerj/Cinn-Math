// PURPOSE: Top-level feature flags shared across the engine.
// SAFE TO CHANGE: Set DEV_MODE to false in production to disable EngineContractValidator.
export const DEV_MODE = true;
export const DEBUG_TELEMETRY = true;
